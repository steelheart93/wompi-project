# docker-compose.yml
version: '3.8'
services:
  db:
    image: postgres:14
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: wompi_user
      POSTGRES_PASSWORD: wompi_password
      POSTGRES_DB: wompi_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: